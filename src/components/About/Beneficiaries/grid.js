import React from "react";
import Grid from "@/components/shared/paralaxBg/gridtext";

const GridUnder = () => {
  return (
    <Grid
      mainHeading="The results speak for themselves."
      subHeading="Since we invest for impact, we let the work teach us. Explore case studies across our  key sectors."
      mainHeadingClass="custom-main-heading-class"
      subHeadingClass="custom-sub-heading-class"
      animationDuration={1500}
      animationDelay={50}
    />
  );
};

export default GridUnder;
